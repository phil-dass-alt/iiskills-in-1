# ─────────────────────────────────────────────────────────────────────────────
# iiskills.in — Nginx reverse-proxy configuration
#
# CLEAN 10 APPS + admin dashboard
# ─────────────────────────────────────────────────────────────────────────────
#   apps/main              → port 3000   https://iiskills.in
#   apps/learn-ai          → port 3002   https://learn-ai.iiskills.in
#   apps/learn-chemistry   → port 3005   https://learn-chemistry.iiskills.in
#   apps/learn-developer   → port 3006   https://learn-developer.iiskills.in
#   apps/learn-geography   → port 3007   https://learn-geography.iiskills.in
#   apps/learn-management  → port 3008   https://learn-management.iiskills.in
#   apps/learn-math        → port 3009   https://learn-math.iiskills.in
#   apps/learn-physics     → port 3010   https://learn-physics.iiskills.in
#   apps/learn-pr          → port 3011   https://learn-pr.iiskills.in
#   apps/learn-apt         → port 3012   https://learn-apt.iiskills.in
#   apps/web (admin)       → port 3040   https://admin.iiskills.in
#
# EXCLUDED (do not proxy):
#   process 0 (logrotate) — not a web process
#   any "copy" apps       — decommissioned
#
# Legacy iiskills.cloud config:
#   Keep in /etc/nginx/sites-available/iiskills.cloud.disabled
#   Do NOT symlink to sites-enabled — prevents port 80/443 collisions
# ─────────────────────────────────────────────────────────────────────────────

# ── Upstreams ─────────────────────────────────────────────────────────────────
upstream main_app         { server 127.0.0.1:3000; }
upstream learn_ai         { server 127.0.0.1:3002; }
upstream learn_chemistry  { server 127.0.0.1:3005; }
upstream learn_developer  { server 127.0.0.1:3006; }
upstream learn_geography  { server 127.0.0.1:3007; }
upstream learn_management { server 127.0.0.1:3008; }
upstream learn_math       { server 127.0.0.1:3009; }
upstream learn_physics    { server 127.0.0.1:3010; }
upstream learn_pr         { server 127.0.0.1:3011; }
upstream learn_apt        { server 127.0.0.1:3012; }
upstream web_admin        { server 127.0.0.1:3040; }

# ── HTTP → HTTPS redirect for ALL iiskills.in domains ────────────────────────
server {
    listen 80;
    server_name
        iiskills.in
        www.iiskills.in
        admin.iiskills.in
        learn-ai.iiskills.in
        learn-chemistry.iiskills.in
        learn-developer.iiskills.in
        learn-geography.iiskills.in
        learn-management.iiskills.in
        learn-math.iiskills.in
        learn-physics.iiskills.in
        learn-pr.iiskills.in
        learn-apt.iiskills.in;

    return 301 https://$host$request_uri;
}

# ── Shared SSL/proxy macro ────────────────────────────────────────────────────
# (Nginx does not have macros; settings are repeated per server block for clarity)

# ── iiskills.in — main landing page (port 3000) ───────────────────────────────
server {
    listen 443 ssl http2;
    server_name iiskills.in www.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://main_app;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── admin.iiskills.in — admin dashboard (port 3040) ──────────────────────────
server {
    listen 443 ssl http2;
    server_name admin.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://web_admin;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-ai.iiskills.in (port 3002) ─────────────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-ai.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_ai;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-chemistry.iiskills.in (port 3005) ──────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-chemistry.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_chemistry;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-developer.iiskills.in (port 3006) ──────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-developer.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_developer;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-geography.iiskills.in (port 3007) ──────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-geography.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_geography;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-management.iiskills.in (port 3008) ─────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-management.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_management;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-math.iiskills.in (port 3009) ───────────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-math.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_math;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-physics.iiskills.in (port 3010) ────────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-physics.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_physics;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-pr.iiskills.in (port 3011) ─────────────────────────────────────────
server {
    listen 443 ssl http2;
    server_name learn-pr.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_pr;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}

# ── learn-apt.iiskills.in (port 3012) — standalone aptitude app ──────────────
server {
    listen 443 ssl http2;
    server_name learn-apt.iiskills.in;

    ssl_certificate     /etc/letsencrypt/live/iiskills.in/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/iiskills.in/privkey.pem;
    include             /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam         /etc/letsencrypt/ssl-dhparams.pem;

    location / {
        proxy_pass         http://learn_apt;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade $http_upgrade;
        proxy_set_header   Connection 'upgrade';
        proxy_set_header   Host $host;
        proxy_set_header   X-Real-IP $remote_addr;
        proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
